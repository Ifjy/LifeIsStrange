# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目状态

**单人业余开发的人生模拟器文字游戏，目前处于设计完成、待启动开发阶段。** 代码尚未初始化，仓库内只有设计文档。

- 平台：PC 网页（横屏），纯前端
- 目标周期：3-6 个月 MVP
- 设计基线（权威）：`docs/specs/2026-07-07-life-simulator-design.md`（V2，当前）
- 早期构想（legacy，仅历史参考）：`docs/specs/2026-07-07-life-simulator-design-v1.md`（V1，已被 V2 取代）

> **`docs/specs/` 下两个文件**：无版本后缀的是当前权威版本，`-v1` 后缀的是被取代的旧版。读设计先读无后缀的。

**任何代码或内容创作前，先读 V2 设计文档。** 它锁定了 MVP 范围、核心机制、架构、数据结构、文件组织与技术选型。

## 技术栈（已锁定）

- Vue 3 + Vite + TypeScript
- 状态管理：Pinia
- 测试：Vitest（仅引擎层做单测）
- 部署：静态资源 CDN（Vercel/Netlify）
- 不使用：后端服务、数据库、路由库（单页应用）

## 架构原则

### 三层分离（必须遵守）

```
内容层 Content  →  引擎层 Engine  →  状态层 State
   (数据)           (流程)            (数据)
```

三层通过明确接口通信，可独立测试。**绝对不要让引擎层持有业务数据，也不要让内容层包含流程逻辑。** 详细数据结构与代码模板见设计文档 §3.2。

### 内容格式：TypeScript 原生对象

事件/结局/铺垫都写成带类型的 TS 文件（`src/content/**/*.ts`）。`condition` 用声明式 DSL（`{attrLt:{体质:30}}`），`apply` 用函数。**不要引入 JSON+DSL 或自研编辑器**——MVP 阶段已决策砍掉。

### 核心机制：出乎意料（Twist System）

这是全游戏灵魂。每个关键选择的 choice 下挂三层 outcome：
- **常规层**：符合直觉的预期结果（高概率）
- **反转层**：讽刺内核的反转（中概率，属性门控）
- **罕见反转层**：脑洞结局（低概率，属性 + 铺垫 flag 双门控）

罕见反转必须有早期"铺垫事件"埋伏笔（`foreshadow_*` flag）。**写新招牌链时必须配套设计铺垫事件**，否则反转就是"烂反转"（纯随机、玩家觉得被坑）。

### Flag 命名规范（严格遵守，防冲突）

| 前缀 | 用途 |
|---|---|
| `foreshadow_*` | 早期铺垫标志 |
| `twist_*` | 反转已触发 |
| `milestone_*` | 人生节点（has_job / married 等） |
| `choice_*` | 重要选择记录 |
| `achievement_*` | 成就（影响评分） |
| `crisis_*` | 阈值事件已触发（防 doom loop 重入） |

## 数值边界与防坑

- 所有属性 **0 ~ 100**（地板防死亡螺旋，天花板防膨胀）
- 属性不会自动衰减，**体质在 35 岁后每年 -1** 是唯一例外
- **死亡螺旋防御**：阈值事件必须给玩家选择权，不能自动 doom。详见设计文档 §5。
- 评分权重（60/15/15/10）和事件权重都是**占位值，playtest 阶段调整**——不要在代码里写死成难以修改的常量。

## MVP 范围红线（不要做）

下列在 V1 设想但 V2 明确砍掉，**MVP 阶段不要实现**：

- 后端服务、用户系统、云存档、排行榜
- 商业化（广告、DLC、付费）
- 社交（好友对比、分享海报——保留可选的最简结局截图导出）
- 事件编辑器（自研 Web 工具）
- 完整像素美术 + 动画（用 emoji + 排版）
- 完整二周目（只做简化版 4 选 1）
- 真正的"穿越重活"玩法段落（招牌链 1 用结局文案带过）

如果用户/任务要求做这些，先确认是否在调整 MVP 范围——不是的话拒绝。

## 内容创作纪律

- **每条招牌反转链必须配铺垫事件**（1-2 行字 + 一个 flag，成本极低）
- **结局优先级数字越大越先判定**（招牌链 > 隐藏 > 常规 > 兜底）
- **结局 ≠ 评分**：结局是叙事（变成什么样的人），评分是数值（活得怎么样）。两者独立。
- 写事件时，参照设计文档 §2.4 的「加班猝死链」代码模板——结构、命名、condition 写法都以它为准。

## 当前任务上下文

设计文档 V2 已经过自审（修复了 4 处歧义：事件密度、阈值触发顺序、nextEvent 语义、招牌链 1 处理）。等待用户最终 review 后，下一步是调用 `superpowers:writing-plans` 技能把设计拆成实现计划。

**如果用户回来继续设计讨论，按 brainstorming 流程走。如果用户要进入实现，调用 writing-plans。**
